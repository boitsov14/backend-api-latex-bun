{
  "scripts": {
    "dev": "bun run --hot src/main.ts",
    "check": "bun biome check --write src",
    "compile": "bun build src/main.ts --compile --target=bun-linux-x64-musl --minify --sourcemap --outfile main"
  },
  "dependencies": {
    "hono": "latest",
    "image-size": "latest"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@tsconfig/bun": "latest",
    "@tsconfig/strictest": "latest",
    "@types/bun": "latest"
  }
}
